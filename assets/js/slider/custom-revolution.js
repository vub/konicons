jQuery(document).ready(function() {
	"use strict";		
					
								
	jQuery('.tp-banner-1').show().revolution({
		delay:9000,
		startwidth:1170,
		startheight:640,
		hideThumbs:10,
		fullWidth:"on",
		forceFullWidth:"on",
		navigationStyle:"preview2"
	});									
});	//ready